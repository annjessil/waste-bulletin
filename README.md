
  # waste bulletin figma

  This is a code bundle for waste bulletin figma. The original project is available at https://www.figma.com/design/QTClKnAOvOErrBT5r1Fnmy/waste-bulletin-figma.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  